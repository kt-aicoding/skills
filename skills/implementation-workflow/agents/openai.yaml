interface:
  display_name: "Implementation Workflow"
  short_description: "Plan, execute, verify, and hand off implementation work"
  default_prompt: "Use $implementation-workflow to turn these requirements into a verified implementation and safe branch handoff."
